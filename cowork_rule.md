# JazzerLife 協作守則（cowork_rule.md）

> 本文件整理 XAVIER 與 Claude 在本專案的協作慣例，供後續開發沿用。
> 最後更新：2026-07-27

---

## 一、語言與溝通風格

- 一律使用**繁體中文（台灣工程用語）**說明。
- 避免冗長開場白與客套話，直接給答案、程式碼與注意事項。
- 回覆保持精簡；能用一句話講完的不寫成一段。
- 完成工作後只用一到兩句交代結果，不逐條複述過程。
- **不確定就問，不要猜**：需求含糊時先用選擇題確認（例如「帳戶分類」的分類值定義、一對一還是一對多），確認後再動手。
- **不擅自做破壞性操作**：刪除檔案、移除功能前先詢問（例如舊路徑導轉頁的去留）。

---

## 二、程式碼撰寫規範

- 修改或撰寫程式碼時，必須提供**完整可執行的 Code Block**，並標明變更路徑與原因。
- 重視防呆與例外處理（Try-Catch）；涉及 API 或資料庫時，需確保資料型態與 Exception 被處理。
- **註解寫「為什麼」，不只寫「做什麼」**。特別是繞過某個坑的程式碼，要把踩到的坑寫清楚，例如：
  - 為何手機版關閉 ag-Grid 的 `resizable`（觸控誤觸縮放熱區導致欄位被壓成 0 寬）
  - 為何日期用字串截取而非 `new Date()`（避免 ISO 字串時區換算導致日期跳掉一天）
  - 為何自訂下拉選單要保留隱藏的原生 `<select>`（維持既有 change 事件與狀態不用改）
- **沿用既有邏輯，不重造輪子**：例如下月預估開支直接使用 `computeMonthlyForecast()`，避免前後端算法不一致。
- **新功能盡量獨立，不動既有計算邏輯**：例如「帳戶分類」與既有的資產／負債判斷是兩套互不干擾的邏輯。
- 選擇器優先權、CSS 覆蓋等技巧要說明原因（例如用 `.ag-theme-quartz-dark` 前綴蓋過全域按鈕規則）。

---

## 三、資料庫變更規範

專案內的程式碼更動外，**如有涉及資料庫的處理，務必把結構及功能另外謄寫一份做備份**。

每次資料庫異動需產出：

1. **SQL 腳本**：放在 `scripts/sql/`，需內含 `IF NOT EXISTS` 防呆、可重複執行。
2. **結構備份文件**：放在 `db_backup/`，記錄欄位定義、索引、對應的 Model／DbContext／API／前端頁面，以及功能說明。
3. **對應的 Model 與 DbContext 設定**同步更新。
4. 明確提醒使用者：**SQL 腳本需自行於 SSMS 手動執行**，未執行前相關 API 會失敗。

參考範例：`scripts/sql/account_category_schema.sql` + `db_backup/account_category_schema_backup_2026-07-27.md`

---

## 四、驗證與品質把關

- 每次改完都要驗證，不能只是改完就說完成：
  - JavaScript：`node --check`
  - CSS：大括號配對檢查
  - HTML：標籤配對檢查（`<section>`／`</section>`、`<div>`／`</div>`）
  - C#：括號配對走查（本機無 dotnet 環境時）
  - JSON：`json.load()` 驗證格式
  - **提供測試環境的重新佈署powershell code & 改動功能之驗證方式**
- **行尾符號務必維持 LF**。工具寫檔可能自動轉成 CRLF，導致 `git diff` 整檔標示為變更、淹沒真正的改動。改完要檢查並用 `sed -i 's/\r$//'` 修正。
- 改動完成後用 `git diff --stat` 確認變更範圍合理，沒有預期外的檔案被波及。
- 非顯而易見的修改，要說明**可能的副作用與影響範圍**。

---

## 五、除錯與問題處理

- **先找根因，不要急著套 workaround**。例如：
  - 「表格欄位重疊」的根因是觸控誤觸欄寬縮放，不是 CSS 寬度不夠
  - 「PWA 裝不了第二個」的根因是 scope 重疊，不是缺 `id` 欄位
- **技術問題有疑慮就查證官方文件**，不要憑印象回答。查到後附上來源連結。
- **判斷錯了要老實承認並修正**：例如 PWA 問題第一次只加了 `id` 沒解決，第二次查官方文件才找到 scope 重疊的真正原因，需明白說出前次判斷不完整。
- **順手發現的既有 bug 要主動回報**，例如切換版本月份不會重新載入資料。
- 修完 bug 要說明「為什麼會發生」，不只是「已修好」。

---

## 六、部署與交付

- 涉及部署時，依 `JazzerLife_環境設定文件` 的標準流程操作：

  ```powershell
  Stop-WebAppPool -Name "<集區名稱>"
  dotnet publish -c Release -o <輸出路徑>
  Start-WebAppPool -Name "<集區名稱>"
  ```

  `dotnet publish` 前務必先停用應用程式集區，否則 DLL 被鎖定會導致失敗。
- 測試機（KAZUO）驗證無誤後，才部署到正式機。
- 交付時要主動提醒**部署前置作業**，例如：
  - 尚未執行的 SQL 腳本
  - PWA manifest 變更後手機端需移除重裝才會生效
- Commit 訊息用條列式整理本階段所有變更，包含資料庫異動與需手動執行的項目。

---

## 七、專案知識庫參照

- 環境設定與部署細節：`JazzerLife_環境設定文件`
- 架構與模組說明：`README`
- 資料庫結構備份：`db_backup/`
- SQL 建置腳本：`scripts/sql/`

---

## 八、前端特別注意事項（本專案踩過的坑）

- **原生 `<select>` 展開清單無法用 CSS 控制**，手機上會跑版；本專案已改用自訂下拉元件（`enhanceSelectDropdown`）。動態重新填充 options 後，記得呼叫 `refreshCustomDropdown()` 同步顯示。
- **ag-Grid 手機版**：關閉 `resizable`，並把 `pinned` 欄位取消固定，避免壓縮可視寬度。
- **PWA 多 App 並存**：同一 origin 下的多個 PWA，scope 必須完全不重疊才能各自安裝；本專案已拆成 `/finance/`、`/car/`、`/macro/` 三個獨立路徑。
- **Highcharts 手機版**：使用 `scrollablePlotArea` 橫向捲動，`scrollPositionX: 0` 預設停在最左邊。
