# 總體經濟溫度計模組 — 正式區部署備註

> 建立日期：2026-07-26
> 適用範圍：`MACRO` schema（總經指標）自測試機（KAZUO）驗證通過後，部署到正式區時的檢查清單。
> 本文件僅為部署備忘，實際操作前請再次確認正式機當下環境狀態。

---

## 1. 資料庫 Schema

- 檔案：`scripts/sql/macro_schema.sql`
- 內容：建立 `MACRO` schema 下 4 張資料表（`EconIndicator`、`EconIndicatorValue`、`EconAlertRule`、`EconAlertLog`）+ 15 筆種子指標（台灣 7 項、美國 8 項）。
- 腳本已用 `IF NOT EXISTS` 包住，可重複執行不會報錯。
- **注意**：測試機的連線字串（`ConnectionStrings:JazzerLife`）本來就指向正式 DB（`192.168.1.101\DESKTOP-E4G7MO5`），代表這份 schema 很可能已經在正式 DB 上跑過了。**部署前務必先用 SSMS 連正式 DB 確認 `MACRO` schema 是否已存在**，避免重複操作或誤判。
- 結構備份文件：`db_backup/macro_schema_backup_2026-07-26.md`（已依專案規範建立，含 rollback SQL）。
- **追加腳本①**：`scripts/sql/macro_schema_add_indicators_2026-07-26.sql`——新增 4 項美國指標（10Y-2Y公債利差、VIX恐慌指數、核心PCE物價指數、消費者信心指數）。備份文件：`db_backup/macro_schema_add_indicators_backup_2026-07-26.md`。
- **追加腳本②**：`scripts/sql/macro_schema_add_market_2026-07-26.sql`——新增 5 項市場資產指標（黃金、比特幣、SP500、費半指數、台股加權指數），Category 統一為「市場」，`MacroCompositeEndpoints.cs` 已改為計算景氣溫度計綜合分數時排除此分類。SP500/比特幣走 FRED；黃金/費半/台股走 Yahoo Finance（新來源，需先驗證，見第 2 節）。
- 以上追加腳本皆用 `IF NOT EXISTS` 個別包住每個 Code，可重複執行、也可只挑其中幾項先跑。

---

## 2. Python 腳本檔案（正式機 scripts 資料夾未進 git，須手動複製）

正式機路徑：`C:\Users\ServerDeployArea\JazzerLife\scripts\`（`PythonRunner.cs` 的 `DefaultScriptsRoot`）

需要複製／更新的檔案：

| 檔案 | 說明 |
|---|---|
| `fetch_fred.py` | 擷取 FRED（美國）資料，純標準函式庫，無需額外套件 |
| `fetch_tw_gov.py` | 擷取台灣官方資料。**部署時務必用最新版本**——這次除了原本的解析邏輯，另外修了三個地方：<br>① 新增 `YYYYMxx` 期別格式解析（如 `1978M01`），否則主計總處 XML 的月資料抓不到；<br>② 遇到 SSL 憑證驗證失敗（`.gov.tw` 站台用 GRCA 憑證，Python 預設信任庫沒有）會自動改用略過驗證的連線重試一次；<br>③ `parse_value()` 支援 `r`/`p` 修正數前綴與 `…` 缺值註記（勞動部 CSV 常見格式） |
| `tw_gov_sources.json` | 資料來源設定。目前 `TW_UNEMPLOYMENT`、`TW_CPI_YOY`、`TW_PPI_YOY`、`TW_GDP_YOY` 已填齊，其餘 3 項台灣指標仍是 TODO 狀態，詳見第 6 節狀態表 |
| `fetch_yahoo.py` | **新增**。擷取 Yahoo Finance Chart API 的股價指數/期貨/加密貨幣日收盤價（黃金、費半指數、台股加權指數）。**非官方 API，無 SLA**，部署前務必先在該機器手動執行驗證連線正常，若長期不穩定需評估替代來源。**已於測試機（KAZUO）驗證連線正常**（2026-07-26，三項指標皆成功抓到資料），正式機仍需另外驗證一次，網路環境可能不同 |

---

## 3. `appsettings.json` 設定（正式機，逐機維護、不進版控）

```json
{
  "FredApiKey": "<正式用的 FRED API Key，需另外申請或確認是否共用測試機那把>",
  "ScriptsRoot": "<僅在正式機路徑不是預設值 C:\\Users\\ServerDeployArea\\JazzerLife\\scripts\\ 時才需要設定>",
  "PythonExePath": "<僅在正式機沒有可用的 venv、需直接指定系統 python.exe 時才需要設定，例如系統路徑而非個人 AppData>"
}
```

- `ScriptsRoot`／`PythonExePath` 若不設定，會自動退回 `PythonRunner.cs` 內建的正式機舊路徑（`...\scripts\venv\Scripts\python.exe`），對正式機是向下相容的，**除非確認正式機的 venv 已經失效，否則不建議動這兩個設定**。
- `FredApiKey` 是必填，沒設定會直接略過 FRED 同步（不會噴錯，但也不會有資料，`SyncFredAsync()` 內有明確的 `LogWarning` 說明）。

---

## 4. IIS App Pool 權限（這次測試機踩到的坑，正式機部署務必檢查）

**症狀**：Hangfire job 顯示「成功」但 Duration 只有個位數~數十毫秒（正常應該要有實際 HTTP 連線的秒級時間），代表 `PythonRunner.RunScript()` 內的 `Process.Start()` 瞬間失敗，例外被 `SyncAllAsync()` 的 try-catch 吞掉，IIS 底下又看不到 Console log，很難察覺。

**根因**：IIS App Pool 身分（如 `IIS AppPool\<PoolName>`）對 `PythonExePath` 指向的 python.exe，以及 `ScriptsRoot` 指向的 scripts 資料夾，沒有讀取＋執行權限。**個人使用者的 AppData 路徑（`C:\Users\<帳號>\AppData\...`）特別容易中招**，因為 App Pool 身分預設完全碰不到別人的使用者目錄。

**部署後務必執行的檢查**：

```powershell
# 1. 確認 App Pool 身分
Import-Module WebAdministration
Get-ItemProperty "IIS:\AppPools\<正式機PoolName>" -Name processModel.identityType

# 2. 補上 python.exe 與 scripts 資料夾的讀取＋執行權限
icacls "<PythonExePath 所在資料夾>" /grant "IIS AppPool\<正式機PoolName>:(OI)(CI)RX" /T
icacls "<ScriptsRoot 資料夾>" /grant "IIS AppPool\<正式機PoolName>:(OI)(CI)RX" /T
```

> 若正式機沿用原本的 `C:\Users\ServerDeployArea\JazzerLife\scripts\venv\Scripts\python.exe`（原本就是給 IIS 用的固定服務帳號路徑），理論上權限應該已經沒問題；此檢查主要是給「有另外覆寫 `PythonExePath`」的情況預留。

---

## 5. 部署步驟順序

1. 確認 / 執行 `macro_schema.sql` + 兩支追加 SQL（見第 1 節）
2. 複製最新版 `fetch_fred.py`、`fetch_tw_gov.py`、`tw_gov_sources.json`、`fetch_yahoo.py` 到正式機 scripts 資料夾（見第 2 節），並先手動驗證 `fetch_yahoo.py` 連線正常
3. 確認 `appsettings.json` 的 `FredApiKey`（必填）、視情況設定 `ScriptsRoot`／`PythonExePath`（見第 3 節）
4. `dotnet publish -c Release -o <正式機發布路徑>`
5. 重啟 App Pool
6. **檢查 App Pool 權限**（見第 4 節，第一次部署此模組務必做）
7. 登入正式站後，於瀏覽器 Console 觸發：
   ```javascript
   fetch('/api/tasks/run-macro-sync', { method: 'POST' })
   ```
8. 到 `/hangfire` 確認該 job 的 Duration 是否合理（應為秒級，而非個位數~數十 ms）
9. 開啟 `macro.html`，確認台灣／美國溫度計皆有數值與燈號

---

## 6. 已知限制（非 Bug，設計上刻意保留）

台灣 7 項指標目前狀態（2026-07-26 更新）：

| 指標 | 狀態 | 來源 |
|---|---|---|
| TW_UNEMPLOYMENT（失業率） | ✅ 已完成 | 主計總處 `ws.dgbas.gov.tw` XML |
| TW_CPI_YOY（CPI年增率） | ✅ 已完成 | data.gov.tw dataset 13228（勞動部彙整，CSV） |
| TW_PPI_YOY（PPI年增率） | ✅ 已完成 | 同上，共用同一份 CSV |
| TW_GDP_YOY（GDP成長率） | ✅ 已完成 | 同上，共用同一份 CSV（實際仍依季公布，非公布月份會略過） |
| TW_CORE_CPI_YOY（核心CPI年增率） | ⏳ 待補 | 尚未找到穩定機器可讀來源，需人工查 stat.gov.tw「消費者物價基本分類指數」 |
| TW_BUSINESS_SIGNAL（景氣對策信號燈分數） | ⏳ 待補 | 已找到來源（data.gov.tw dataset 6099，國發會），但檔案格式是 ZIP，`fetch_tw_gov.py` 目前只支援 xml/csv，需先擴充腳本支援解壓縮才能啟用 |
| TW_EXPORT_ORDERS_YOY（外銷訂單年增率） | ⏳ 待補 | 尚未找到穩定機器可讀來源，需人工查經濟部統計處（service.moea.gov.tw） |

未完成的 3 項，`fetch_tw_gov.py` 會主動略過並於 stderr 說明原因，**不會寫入猜測資料**。之後補齊來源時，只需更新 `tw_gov_sources.json`（若是 ZIP 來源則還需擴充腳本），不需動其他程式碼。
